# calculator_rs 仕様書

## 1. プロジェクト概要

### 1.1 目的

Rust による電卓自作を通じて、**字句解析(Lexer)・構文解析(Parser)・評価(Evaluator)** という言語処理系の基礎パイプラインを理解することを目的とした、教育用ミニマル電卓。

### 1.2 非目標(Non-Goals)

- 複素数・行列などの高度な数値型
- 完全な数値精度保証（`f64` の範囲で十分）
- スクリプトファイルの実行

### 1.3 想定ユーザ

自分自身（学習者）。エンドユーザ向けプロダクトではない。

---

## 2. 処理パイプライン

```
入力文字列
  → Lexer（字句解析）→ トークン列
  → Parser（構文解析）→ AST（抽象構文木）
  → Evaluator（評価）→ 結果（f64）
```

ブラウザ自作と同じ「文字列を段階的に構造化する」流れ。

---

## 3. モジュール構成

```
src/
├── main.rs       # エントリポイント。eguiアプリの起動
├── lexer.rs      # 字句解析（文字列 → トークン列）
├── parser.rs     # 構文解析（トークン列 → AST）
├── evaluator.rs  # 評価（AST → f64）
└── ui.rs         # egui によるGUI（ボタン・ディスプレイ）
```

---

## 4. マイルストーン

### M1: 足し算・引き算

- `1 + 2`、`10 - 3` が計算できる
- Lexer・Parser・Evaluator の基本構造を作る
- REPL（入力 → 結果表示のループ）で動作確認

### M2: 掛け算・割り算と演算子優先順位

- `1 + 2 * 3` → `7`（掛け算が先）
- 優先順位を Parser で表現する

### M3: カッコ

- `(1 + 2) * 3` → `9`

### M4: 負の数・小数

- `-1.5 * 2` → `-3`
- 単項マイナスの対応

### M5: 変数（保留）

- `x = 3` で代入、`x + 1` → `4`
- 変数テーブル（`HashMap`）を Evaluator に持たせる

### M6: 組み込み関数（保留）

- `sqrt(4)` → `2`、`abs(-3)` → `3` など

---

## 5. GUI（egui）

- eframe（eguiのウィンドウフレームワーク）を使用
- 数字ボタン・演算子ボタン・`=`・`C`（クリア）を配置
- 入力式と結果をディスプレイ部分に表示

---

## 6. エラーハンドリング方針

| レイヤ    | 方針                                       |
| --------- | ------------------------------------------ |
| Lexer     | 未知の文字はエラーメッセージを表示して続行 |
| Parser    | 構文エラーはメッセージを表示して続行       |
| Evaluator | ゼロ除算などはメッセージを表示して続行     |

`panic!` は使わない。REPL は1行エラーが出ても終了しない。
